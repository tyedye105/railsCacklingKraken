<%= render "/layouts/errors", :object => @comment %>
<%= form_for [@product, @comment] do |f| %>
  <%= f.hidden_field :user_id, :value => @user.id %>
  <div class="form-group">
  <%= f.label :comment, "Say it here:" %><br>
  <%= f.text_area :comment %>
  </div>
  <%= f.submit %>
  <% end %>
</div>
